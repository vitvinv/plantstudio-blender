{
    "tasks": [
        {
            "type": "shell",
            "label": "Build distribution zip",
            "command": "python",
            "args": [
                "scripts/release.py",
                "--zip-only"
            ],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "description": "Package the addon folder into the installable plantstudio_blender.zip (excludes caches)"
        },
        {
            "type": "shell",
            "label": "Release addon to GitHub",
            "command": "python",
            "args": [
                "scripts/release.py"
            ],
            "problemMatcher": [],
            "description": "Build zip and create/update the GitHub release at the current bl_info version"
        }
    ],
    "version": "2.0.0"
}